# Arquivo: .env.example
# Copie este arquivo para .env e preencha com seus próprios valores.
# NUNCA envie o arquivo .env para o repositório.

# === Configs do PostgreSQL (Usado pela API e pelo Kong) ===
POSTGRES_DB=sinapi
POSTGRES_USER=admin
POSTGRES_PASSWORD=change-in-production-db-pass # TROCAR POR UMA SENHA FORTE

# === Configs do Kong (API Gateway) ===
# O Kong precisa de seu próprio usuário e banco de dados para armazenar suas configurações.
KONG_DB_USER=kong
KONG_DB_PASSWORD=change-in-production-kong-pass # TROCAR POR UMA SENHA FORTE
KONG_PG_HOST=db # Nome do serviço do PostgreSQL no docker-compose

# === Configs da API (FastAPI) ===
# URL de conexão que a API usará para se conectar ao banco de dados.
# Use as mesmas credenciais do POSTGRES_USER e o host 'db'.
DATABASE_URL=postgresql://admin:change-in-production-db-pass@db:5432/sinapi1~# Arquivo: .env.example
# Copie este arquivo para .env e preencha com seus próprios valores.
# NUNCA envie o arquivo .env para o repositório.

# === Configs do PostgreSQL (Usado pela API e pelo Kong) ===
POSTGRES_DB=sinapi
POSTGRES_USER=admin
POSTGRES_PASSWORD=change-in-production-db-pass # TROCAR POR UMA SENHA FORTE

# === Configs do Kong (API Gateway) ===
# O Kong precisa de seu próprio usuário e banco de dados para armazenar suas configurações.
KONG_DB_USER=kong
KONG_DB_PASSWORD=change-in-production-kong-pass # TROCAR POR UMA SENHA FORTE
KONG_PG_HOST=db # Nome do serviço do PostgreSQL no docker-compose

# === Configs da API (FastAPI) ===
# URL de conexão que a API usará para se conectar ao banco de dados.
# Use as mesmas credenciais do POSTGRES_USER e o host 'db'.
DATABASE_URL=postgresql://admin:change-in-production-db-pass@db:5432/sinapi
